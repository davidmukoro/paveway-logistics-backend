from ..utils import get_vendor_data,get_vendor_mocked_data

def fetch_vendor_orders(vendor):
    #return get_vendor_data(vendor)
    #replace this later with the one up
    return get_vendor_mocked_data(vendor)